from project import db 
from project.models import BlogPost

# Insert data
db.session.add(BlogPost("Blog post1", "First post", 7))
db.session.add(BlogPost("Blog post2", "Second post", 7))

# Commit changes
db.session.commit()
